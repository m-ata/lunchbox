import React, { useState } from 'react';

import { View,   
    Text, 
    TouchableOpacity, 
} from 'react-native';

const Pickup = () => {

    return (
        <View>
            <Text>Pick</Text>
        </View>
    )
};

export default Pickup;